import { FilterRoundsPipe } from './filter-rounds.pipe';

describe('FilterRoundsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRoundsPipe();
    expect(pipe).toBeTruthy();
  });
});
